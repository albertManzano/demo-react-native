import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    spinner: {
        width: 200,
        height: 200,
        alignSelf:"center",
        marginTop: '40%'
    }
});

export default styles;